{
  "name": "dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "typecheck": "pnpm -r typecheck",
    "start": "node start-services.js",
    "start:backend": "RUN_MODE=backend-only node start-services.js",
    "start:frontend": "RUN_MODE=frontend-only node start-services.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "typescript": "^5.0.0"
  }
}
